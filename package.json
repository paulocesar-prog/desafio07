{
  "name": "readtime-js",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --testPathIgnorePatterns=jest.config.mjs"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  }
}